# alt-python-boot-aws-lambda

AWS Lambda adapter for the alt-python/boot framework.

Provides `LambdaAdapter`, `LambdaControllerRegistrar`, and `lambda_starter()` to wire
CDI-managed controllers into an API Gateway HTTP API v2 Lambda handler.

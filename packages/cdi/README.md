# alt-python-cdi

Spring-inspired CDI (Context and Dependency Injection) for Python.

Provides `ApplicationContext`, `Singleton`, `Prototype`, `Context`, `Component`, `Property`, and `Scopes` — a synchronous, profile-aware dependency injection container.

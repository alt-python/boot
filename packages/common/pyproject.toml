[project]
name = "alt-python-common"
version = "1.1.0"
description = "Shared utilities for alt-python packages"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["common"]

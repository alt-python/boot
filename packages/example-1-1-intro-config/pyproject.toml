[project]
name = "example-1-1-intro-config"
version = "1.1.0"
description = "Step 1.1 — alt-python/config standalone introduction"
requires-python = ">=3.12"
dependencies = [
    "alt-python-config",
]

[tool.uv.sources]
alt-python-config = { workspace = true }

[project]
name = "example-1-2-intro-logger"
version = "1.0.0"
description = "Step 1.2 — alt-python/logger + config"
requires-python = ">=3.12"
dependencies = [
    "alt-python-config",
    "alt-python-logger",
]

[tool.uv.sources]
alt-python-config = { workspace = true }
alt-python-logger = { workspace = true }
